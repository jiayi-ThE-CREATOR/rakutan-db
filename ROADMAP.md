# ラクハン ロードマップ v6（2026-08-18 改訂）

> **この版が最新です。** v5 までの PDF（企画書・ロードマップ）は内容が古いので参照しないでください。
> 変更したら**この1ファイルだけ**を直します。

|  |  |
|---|---|
| **公開** | 2026-08-26（水） ── あと **8日** |
| **履修登録開始** | 2026-09-02（水） ── あと **15日** ／ **これが本当の目標日** |
| **体制** | **6人**（阪大4・京大2）。王立明さん離脱にともない再配分 |
| **本番** | https://rakutan-db.wjy20050815.workers.dev （公開前・学内拡散はまだしない） |

---

## 1. 結論 ── 残っているリスクは中身だけ

コードは予定より進んでいます。8/18 時点で PR は全部マージ済み、本番も動いています。
**残りの8日で足りないのは口コミだけです。**

### 口コミは「まず量」。科目は選ばない

書いてくれる人が何人いるか、まだ分かりません。
**人数の保証がない状態で「この134件を書いてください」と指定はできません。**
自分が取ったことのない科目は書けないからです。

なので **フェーズを分けます。**

| | やること | いつ |
|---|---|---|
| **フェーズ1（いま）** | **とにかくゼロから増やす。科目は問わない。**自分が取った科目を書いてもらう | 〜8/26 |
| **フェーズ2** | 規模が出てから、おすすめ上位134件を狙って埋める | 公開後 |

### ただし、量だけでは「一番目立つ場所」は直りません

サイトが最初に薦める134件のうち130件は「テストの難しさが未検証」です。
科目を選ばずに集めると、そこに当たる確率は **134 / 1,112 = 12%** しかありません。

```
口コミ 100件 → 上位134件に当たるのは期待値 12件
口コミ 200件 → 期待値 24件
口コミ 300件 → 期待値 36件
```

**つまり、量を増やしても上位は埋まりません。** ここは収集ではなく**表示で解きます。**

### 解き方：検証ずみの科目を上に出す

いま **すでに完全に検証ずみの科目が158件** あります。Top100 を埋めるには十分です。

```
現状の内訳（1,112件）
  難しさ未検証（pending）  502
  判定ずみ                 158   ← ここだけで上位は埋まる
  情報不足                 552
```

**おすすめ順を「検証ずみ → 未検証」の順に変えれば、
どの科目に口コミが来ても、最初の画面は常に根拠のあるものになります。**

> **⚠ これは未決定です。** ランキングの並び順はサービスの核なので、wang の一存では変えません。
> 決めてから実装します。

## 2. 王立明さん離脱の影響 ── 数えたところ、ほぼ無い

担当されていた `scrape/parse.py` の `METHOD_RULES`（成績評価の項目名を分類する規則）の
未分類は **61種類**。多く聞こえますが、実際に影響するのは **64件（全体の5.8%）** です。

61種類を全部分類して再採点した結果：

```
判定できる科目   543 → 546   （+3件）
```

**+3件のために8日間を使いません。人も補充しません。**

| 引き継ぐもの | 行き先 | いつ |
|---|---|---|
| `METHOD_RULES` 61種類の分類 | wang | **公開後**（8/27〜） |
| データ品質チェック | 政岡 | 8/19〜。全件取得をやった本人が一番データを知っている |

### ⚠️ ここを触る人への注意

**`report` に分類すると、判定できる科目はむしろ減ります。**
レポート軸は本数・字数が必要ですが、それはシラバスに載っていないためです。
重みだけ `report` に移って、その軸が測れず `COVERAGE_MIN` を割ります。

**分類するなら `attendance` 側から。** 実測でも `attendance` だけが +3、`report` は ±0 でした。

---

## 3. 6人の割り当て

| 担当 | 8/26 までにやること | 状態 |
|---|---|---|
| **しゅんや**<br>阪大・口コミ | **口コミ収集（科目は問わない・とにかく量）**。フォームに**「テストの難しさ」の設問が必須**（難しい=2／ふつう=1／易しい=0／試験なしは空欄）。**これが無いと130件は永久に埋まりません** | **最優先** |
| **笠井**<br>阪大・LINE | LINE公式アカウントのチャネル発行 → きむらへ受け渡し | **8/19まで** |
| **きむら**<br>阪大・LINE | LINE Bot 疎通。データは公開URLの `preset_top` を読むだけ。**採点ロジックは書かなくていい** | 8/21 目標 |
| **松下**<br>京大・UI | 本番（実機）でのスマホ確認。口コミが入り始めてからの表示崩れ | 8/22〜 |
| **政岡**<br>京大・データ | データ品質チェック（王立明さんから引き継ぎ）。取りこぼし・文字化け・重複 | 8/19〜 |
| **wang**<br>阪大・PM/採点 | 口コミが入るたび再ビルド。公開日の `noindex` 解除。`METHOD_RULES` は公開後 | 随時 |

**全員へ：** 口コミは1人1科目でも入れてください。
自分が去年取った科目が、そのまま130件のうちの1件です。

---

## 4. 日程

| 日 | やること |
|---|---|
| **8/18（火）** ✅ | 本番公開・全PRマージ済み。転送量 1.4MB→**83KB**（gzip・大阪配信）。マージから**約80秒で自動反映**。**まだ学内には拡散しない**（`noindex` 中） |
| **8/19（水）** | 口コミ集中スタート。**笠井→きむらのチャネル受け渡し最終ライン**。政岡さんのデータ品質チェック開始 |
| **8/20（木）〜8/21（金）** | **LINE 疎通**。Bot が公開URLの `preset_top` を読んで返せる状態に。8/21 が目標 |
| **8/22（土）〜8/24（月）** | 口コミの追い込み。松下さんの実機確認 |
| **8/25（火）21:00** | **公開可否の判定（Go / No-Go）** ── 下の基準で判断 |
| **8/26（水）** 🚀 | **公開。`noindex` を外す**（後述）。その後に学内へ拡散 |
| **8/27（木）〜9/1（火）** | 監視と追加。口コミが増えるたび再ビルド。`METHOD_RULES` の61種類はここで消化 |
| **9/2（水）** 🎯 | **履修登録開始・アクセスのピーク。この日に動いていることが最終目標** |

### 🚨 8/26 に最初にやること

```bash
git rm web/robots.txt
# web/_headers から「X-Robots-Tag」の行を削除
```

**これを忘れると、公開してもGoogleに一切載りません。**
`wang` が当日動けない場合に備えて、**この手順は2人以上が知っていること**を判定基準に入れています。

---

## 5. 8/25 21:00 の判定基準

**v5 の「上位134件のうち100件」は取り下げました。** 科目を選んで集められない以上、
達成不可能な基準だったためです。量で測る形に変えます。

### 必須（1つでも欠けたら公開を遅らせる）

- [ ] **口コミが100件以上ある** ── いま0件。**6人 × 20科目 = 120件**で届きます
- [ ] **おすすめ上位100件が「検証ずみ」で埋まっている** ── 並び順を変えれば今日でも達成できます（検証ずみが158件あるため）
- [ ] **LINE から検索結果が返る** ── 入口がこれ。無いと「知ってもらう手段」が無い
- [ ] **`noindex` 解除の手順を2人以上が知っている**

### 推奨

- [ ] 口コミ 200件以上
- [ ] 実機（iPhone・Android 各1台）で確認済み ── 利用のほぼ全部がスマホ

### 公開日は動かせます

**8/26 は自分たちで決めた日で、外部との約束ではありません。**
外部との約束は **9/2（履修登録開始）** だけです。

中身が薄いまま出すと、最初に見た人はもう戻ってきません。
**基準を満たさない場合、8/26 の公開は遅らせます。** 9/2 に間に合えば目的は達成できます。

---

## 6. やらないと決めたこと（再確認）

- **サイト内からの口コミ投稿**（D1 + Functions）── 公開後。いまは入口ごと隠してあります
- **独自ドメイン** ── いまの `*.workers.dev` で公開する
- **過去問の掲載** ── 著作権は出題した教員にある。載せるのは「試験の傾向」まで
- **教員を軸にした集計・ランキング・検索** ── 評価の対象は科目のみ。詳細は README「教員名の扱い」

---

## 7. v5 から変えたこと

- **サービス名を「ラクハン」に決定**（それまでは「楽単DB（仮）」）
- **口コミ収集を「量が先」に変更。** 上位134件を狙う方針は**公開後（フェーズ2）**へ
  ── 書いてくれる人数が読めない段階で科目を指定できないため
- **8/25 の判定基準を書き換え。** 「上位134件のうち100件」→ **「口コミ100件以上」**
- **おすすめ順を「検証ずみを先に」に変える案を追加**（1章／**未決定**）
  ── 量を増やしても上位は埋まらないため、収集ではなく表示で解く

### v4 から変えたこと（v5 で決定ずみ）

- 7人 → **6人**。王立明さんの担当を2人に分割し、**補充はしない**と決めた
- `METHOD_RULES` を **公開後** に後ろ倒しした（+3件のため）
- **8/25 21:00 に Go / No-Go の判定**を置き、**公開日は動かせる**ことを明示した
- `server.py` の担当を wang として担当表に明記した

---

*数値はすべて 2026-08-18 時点の `data/courses.json`（1,112件）を実際に採点して出したものです。*

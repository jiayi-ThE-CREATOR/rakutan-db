# ラクハン ロードマップ v6（2026-08-18 改訂）

> **この版が最新です。** v5 までの PDF（企画書・ロードマップ）は内容が古いので参照しないでください。
> 変更したら**この1ファイルだけ**を直します。

|  |  |
|---|---|
| **公開** | 2026-08-26（水） ── あと **8日** |
| **履修登録開始** | 2026-09-02（水） ── あと **15日** ／ **これが本当の目標日** |
| **体制** | **6人**（阪大4・京大2）。王立明さん離脱にともない再配分 |
| **UI 作り直し v2** | 2026-08-22 決定。PC 3カラム／About ページ／ブランド統一／オープニング演出 ── **8章** |
| **本番** | https://rakutan-db.wjy20050815.workers.dev （公開前・学内拡散はまだしない） |

---

## 1. 結論 ── 残っているリスクは中身だけ

コードは予定より進んでいます。8/18 時点で PR は全部マージ済み、本番も動いています。
**残りの8日で足りないのは口コミだけです。**

### 口コミは「まず量」。科目は選ばない

書いてくれる人が何人いるか、まだ分かりません。
**人数の保証がない状態で「この134件を書いてください」と指定はできません。**
自分が取ったことのない科目は書けないからです。

なので **フェーズを分けます。**

| | やること | いつ |
|---|---|---|
| **フェーズ1（いま）** | **とにかくゼロから増やす。科目は問わない。**自分が取った科目を書いてもらう | 〜8/26 |
| **フェーズ2** | 規模が出てから、おすすめ上位134件を狙って埋める | 公開後 |

### ただし、量だけでは「一番目立つ場所」は直りません

サイトが最初に薦める134件のうち130件は「テストの難しさが未検証」です。
科目を選ばずに集めると、そこに当たる確率は **134 / 1,112 = 12%** しかありません。

```
口コミ 100件 → 上位134件に当たるのは期待値 12件
口コミ 200件 → 期待値 24件
口コミ 300件 → 期待値 36件
```

**つまり、量を増やしても上位は埋まりません。** ここは収集ではなく**表示で解きます。**

### 解き方：検証ずみの科目を上に出す

いま **すでに完全に検証ずみの科目が158件** あります。Top100 を埋めるには十分です。

```
現状の内訳（1,112件）
  難しさ未検証（pending）  502
  判定ずみ                 158   ← ここだけで上位は埋まる
  情報不足                 552
```

**おすすめ順を「検証ずみ → 未検証」の順に変えれば、
どの科目に口コミが来ても、最初の画面は常に根拠のあるものになります。**

> **⚠ これは未決定です。** ランキングの並び順はサービスの核なので、wang の一存では変えません。
> 決めてから実装します。

## 2. 王立明さん離脱の影響 ── 数えたところ、ほぼ無い

担当されていた `scrape/parse.py` の `METHOD_RULES`（成績評価の項目名を分類する規則）の
未分類は **61種類**。多く聞こえますが、実際に影響するのは **64件（全体の5.8%）** です。

61種類を全部分類して再採点した結果：

```
判定できる科目   543 → 546   （+3件）
```

**+3件のために8日間を使いません。人も補充しません。**

| 引き継ぐもの | 行き先 | いつ |
|---|---|---|
| `METHOD_RULES` 61種類の分類 | wang | **公開後**（8/27〜） |
| データ品質チェック | 政岡 | 8/19〜。全件取得をやった本人が一番データを知っている |

### ⚠️ ここを触る人への注意

**`report` に分類すると、判定できる科目はむしろ減ります。**
レポート軸は本数・字数が必要ですが、それはシラバスに載っていないためです。
重みだけ `report` に移って、その軸が測れず `COVERAGE_MIN` を割ります。

**分類するなら `attendance` 側から。** 実測でも `attendance` だけが +3、`report` は ±0 でした。

---

## 3. 6人の割り当て

| 担当 | 8/26 までにやること | 状態 |
|---|---|---|
| **しゅんや**<br>阪大・口コミ | **口コミ収集（科目は問わない・とにかく量）**。フォームに**「テストの難しさ」の設問が必須**（難しい=2／ふつう=1／易しい=0／試験なしは空欄）。**これが無いと130件は永久に埋まりません** | **最優先** |
| **笠井**<br>阪大・LINE | LINE公式アカウントのチャネル発行 → きむらへ受け渡し | **8/19まで** |
| **きむら**<br>阪大・LINE | LINE Bot 疎通。データは公開URLの `preset_top` を読むだけ。**採点ロジックは書かなくていい** | 8/21 目標 |
| **松下**<br>京大・UI | 本番（実機）でのスマホ確認。口コミが入り始めてからの表示崩れ | 8/22〜 |
| **政岡**<br>京大・データ | データ品質チェック（王立明さんから引き継ぎ）。取りこぼし・文字化け・重複 | 8/19〜 |
| **wang**<br>阪大・PM/採点 | 口コミが入るたび再ビルド。公開日の `noindex` 解除。`METHOD_RULES` は公開後 | 随時 |

**全員へ：** 口コミは1人1科目でも入れてください。
**科目は選ばなくていいです。**自分が去年取った科目を、そのまま書いてください。

---

## 4. 日程

| 日 | やること |
|---|---|
| **8/18（火）** ✅ | 本番公開・全PRマージ済み。転送量 1.4MB→**83KB**（gzip・大阪配信）。マージから**約80秒で自動反映**。**まだ学内には拡散しない**（`noindex` 中） |
| **8/19（水）** | 口コミ集中スタート。**笠井→きむらのチャネル受け渡し最終ライン**。政岡さんのデータ品質チェック開始 |
| **8/20（木）〜8/21（金）** | **LINE 疎通**。Bot が公開URLの `preset_top` を読んで返せる状態に。8/21 が目標 |
| **8/22（土）〜8/24（月）** | 口コミの追い込み。松下さんの実機確認 |
| **8/25（火）21:00** | **公開可否の判定（Go / No-Go）** ── 下の基準で判断 |
| **8/26（水）** 🚀 | **公開。`noindex` を外す**（後述）。その後に学内へ拡散 |
| **8/27（木）〜9/1（火）** | 監視と追加。口コミが増えるたび再ビルド。`METHOD_RULES` の61種類はここで消化 |
| **9/2（水）** 🎯 | **履修登録開始・アクセスのピーク。この日に動いていることが最終目標** |

### 🚨 8/26 に最初にやること

> **✅ 2026-08-26 に実施ずみ**（ブランチ `feat/wang-open-index`）。以下は記録です。
> 実際にやったのは2手だけではありません ―― 外した瞬間に「同じ本文の重複URL」
> （旧ドメインと計測リンク14本）が一緒に載ってしまうため、canonical・sitemap・
> それぞれの noindex を同じ変更で入れています。`node tools/test_index_gate.mjs` が見張り。

```bash
git rm web/robots.txt
# web/_headers から「X-Robots-Tag」の行を削除
```

**これを忘れると、公開してもGoogleに一切載りません。**
`wang` が当日動けない場合に備えて、**この手順は2人以上が知っていること**を判定基準に入れています。

---

## 5. 8/25 21:00 の判定基準

**v5 の「上位134件のうち100件」は取り下げました。** 科目を選んで集められない以上、
達成不可能な基準だったためです。量で測る形に変えます。

### 必須（1つでも欠けたら公開を遅らせる）

- [ ] **口コミが100件以上ある** ── いま0件。**6人 × 20科目 = 120件**で届きます
- [ ] **おすすめ上位100件が「検証ずみ」で埋まっている** ── 並び順を変えれば今日でも達成できます（検証ずみが158件あるため）
- [ ] **LINE から検索結果が返る** ── 入口がこれ。無いと「知ってもらう手段」が無い
- [ ] **`noindex` 解除の手順を2人以上が知っている**

### 推奨

- [ ] 口コミ 200件以上
- [ ] 実機（iPhone・Android 各1台）で確認済み ── 利用のほぼ全部がスマホ

### 公開日は動かせます

**8/26 は自分たちで決めた日で、外部との約束ではありません。**
外部との約束は **9/2（履修登録開始）** だけです。

中身が薄いまま出すと、最初に見た人はもう戻ってきません。
**基準を満たさない場合、8/26 の公開は遅らせます。** 9/2 に間に合えば目的は達成できます。

---

## 5.5 公開後に持ち越したもの（2026-08-26 に決めた）

**9/2（履修登録のピーク）を越えてから着手する。**「やらない」ではなく「いまはやらない」。

| 持ち越し | いまの状態 | なぜ後回しか |
|---|---|---|
| **科目ごとの静的ページ**（SEO の本丸） | `?c=<id>` は `index.html` を返すだけ。title も description もトップと同じで、canonical も `/` を指す ―― **sitemap に 7,877件並べても Google は全部トップの重複として捨てる** | 効かせるには `build.py` で科目ごとのページを焼き、4軸の数値・担当教員・口コミ件数を HTML に直接載せる必要がある。URL規則・教員名の線引き・デプロイ容量など決めることが多い。「科目名 楽単」で検索してくる人を拾えるかはこれ次第 |
| **旧ドメインの 301** | canonical だけで守っている | Worker のコードでは解けない（試して本番を2分20秒止めた。HANDOFF 先頭）。独自ドメインが Cloudflare を向いておらず nginx 経由のため。**吉村さん側の作業**が要る |
| **Search Console 登録** | 未登録。`sitemap.xml` は出してある | 王のブラウザで「URL プレフィックス」＋「HTML タグ」で所有権確認 → token を `templates/shell.html` に入れる |
| **「私の時間割」保存** | 未着手 | 8/22 の UI 作り直しでスコープ外にした新機能。localStorage で足りる |
| **LINE から来た人の既定絞り込み** | 未着手 | LINE Bot が返すリンクにクエリ（`?year=2&sem=haru`）を載せるだけ。復元側の実装が要る |

---

## 6. やらないと決めたこと（再確認）

- **サイト内からの口コミ投稿**（D1 + Functions）── 公開後。いまは入口ごと隠してあります
- **独自ドメイン** ── いまの `*.workers.dev` で公開する
- **過去問の掲載** ── 著作権は出題した教員にある。載せるのは「試験の傾向」まで
- **教員を軸にした集計・ランキング・検索** ── 評価の対象は科目のみ。詳細は README「教員名の扱い」

---

## 7. v5 から変えたこと

- **サービス名を「ラクハン」に決定**（それまでは「楽単DB（仮）」）
- **口コミ収集を「量が先」に変更。** 上位134件を狙う方針は**公開後（フェーズ2）**へ
  ── 書いてくれる人数が読めない段階で科目を指定できないため
- **8/25 の判定基準を書き換え。** 「上位134件のうち100件」→ **「口コミ100件以上」**
- **おすすめ順を「検証ずみを先に」に変える案を追加**（1章／**未決定**）
  ── 量を増やしても上位は埋まらないため、収集ではなく表示で解く

### v4 から変えたこと（v5 で決定ずみ）

- 7人 → **6人**。王立明さんの担当を2人に分割し、**補充はしない**と決めた
- `METHOD_RULES` を **公開後** に後ろ倒しした（+3件のため）
- **8/25 21:00 に Go / No-Go の判定**を置き、**公開日は動かせる**ことを明示した
- `server.py` の担当を wang として担当表に明記した

---

## 8. UI 作り直し v2 ── 設計（2026-08-22 決定 / wang）

> **この章は「見た目の改善」ではありません。土台の作り直しです。**
> ラクハンを GUILD の「阪大生の衣食住」サービス群の第1号として外に出すため、
> PC レイアウト・ブランド露出・ページ構成・オープニング演出をまとめて作り直します。
> ここは合意ずみの設計です。実装計画は別に作ります。

### 8.0 きっかけ ── まず事実確認

- `web/index.html` に**幅ベースの `@media` が1つも無い**（あるのは `prefers-color-scheme` の2つだけ）。
  `.wrap` `.hd` `.sheet .inner` がいずれも `max-width:560px`。
  **PC でもスマホと同じ 560px の1列**。PC 用のレイアウトは一度も作られていません。
- `tools/shots.mjs` は22日間ずっと 1280px の PC スクショ（`02-top-desktop`）を撮っていました。
  見えてはいた。バグとして扱われていなかっただけです。
- 1,112件の一覧は sentinel による**無限スクロールのみ**（`onScrollCheck`）。終点が無く、現在位置も分からない。
- ブランド色 `#DB6209` はロゴだけ。UI の主色は緑 `--go:#0e7c66`。**主色が2つある状態**（旧 B-1）。
- フッター／About に「学生団体 GUILD が運営しています」が無い（旧 B-2）。ブランド資料のレッドライン未対応。

### 8.1 決めたこと

| # | 決定 |
|---|---|
| 1 | PC は**3カラムのワークベンチ**（左：絞り込み／中：一覧／右：詳細） |
| 2 | 色を**「ブランド・操作色」と「データ目盛り色」の2系統**に分ける（旧 B-1 を解決） |
| 3 | ヘッダのロゴ下に `Designed by GUILD` ／ `AI Community` の2行 |
| 4 | トップは**ヒーロー → スクロールでワークベンチ**。ナビは3項目 |
| 5 | `About ラクハン` を**独立した HTML ファイル**として新設（旧 B-2 を解決） |
| 6 | 一覧は**無限スクロールを廃止し、ページングへ** |
| 7 | **筆順で書き出すオープニング演出**（約1.4秒、再訪時はスキップ） |
| 8 | `web/index.html` を**複数ファイルに分割**し、共有の外殻は `build.py` が注入する |

### 8.2 「動かしてはいけない前提」を1つ書き換えます

`CLAUDE.md` の「モバイルファースト ―― 履修登録はスマホでやる。**PCで綺麗でも意味がない**」。

前半は正しい。**後半は間違いです。** 実際に PC で見に来る学生はいますし、
GUILD として外に見せるとき最初に開かれるのは PC です。次の文に差し替えます。

> **モバイルファースト（PC も必須）** ―― 履修登録の大半はスマホ。だから CSS のベースは
> モバイルで書き、PC は `min-width` で上乗せする。ただしこれは「PC は後回しでいい」という
> 意味ではない。**PC は第2の必須形態**として作り込む。

**「空の検索窓を最上部に置かない」は残します。** 入口は空きコマグリッドのままです。
ただし検索窓は消しません。**「条件」ブロックと一覧のあいだのツールバー**
（いま件数と並び替えが載っている `.bar`）へ移します。

理由：「自分が取った／取る予定の科目が、楽に単位を取れるのか確かめたい」という
**具体的な科目名を持って来る使い方は実在する**ためです。
最上部から外すことで入口の原則は守り、一覧に隣接させることで
「件数・並び替えと同種の、一覧に効く道具」という位置づけにします。

> なお現状の `web/index.html` では検索窓が `.wrap` の**先頭**にあり、
> この原則を実装のほうが破っています。今回あわせて直します。

### 8.3 レイアウト

| 段 | 幅 | 形 |
|---|---|---|
| base | 〜767 | いまの1列。**ほぼ変更なし** |
| md | 768〜1023 | 2カラム（左：絞り込み／右：一覧）。詳細は右からのドロワー |
| lg | 1024〜1439 | 3カラム `260px / minmax(420px,1fr) / 380px` |
| xl | 1440〜 | 3カラム `280px / 1fr / 440px`、全体 `max-width:1560px` |

- 3カラムはそれぞれ独立スクロール。左右は sticky。**ページ送りで動くのは中カラムだけ**
- 状態を1つ増やす：`selectedCourseId`
  - スマホ：いまと同じくカード内に `.detail` を開く（挙動そのまま）
  - PC：同じ描画関数の出力を右カラムへ差す。`matchMedia('(min-width:1024px)')` で分岐し、
    **描画関数は1本のまま**にする（2重実装をしない）
- **右カラムの空状態**には「4軸と信頼度の読み方」のミニ凡例を出す。
  数字を読む直前の人に、いちばん必要な説明が出る場所になる

### 8.4 デザイントークン

いまの CSS は `padding:11px 13px` `border-radius:9px` `font-size:11.5px` `margin-top:26px` のように
**その場で決めた裸の数値だらけ**です。色の不統一より、これが「部品ごとに別々に作った感」の主因です。

**色 ── 2系統に分け、混ぜて使わない**

```
/* ブランド・操作色 ── 「押せるもの」にだけ使う */
--brand:#DB6209   --brand-ink:#fff   --brand-soft:(淡いオレンジ地)   --focus:(brand 40%)

/* データ目盛り ── 4軸バーと band にだけ使う。操作色には使わない */
--scale-light:#0e7c66   --scale-mid:#b8862c   --scale-heavy:#C0392B
```

- いまの `--go` 14箇所を役割で振り分ける。
  チップ選択／FAB／相性の数字／リンク／フォーカスリング → `--brand`、
  バーの塗り／band ラベル → `--scale-*`
- **`--warn` を `#b4532a` → `#C0392B` へ。** ブランドオレンジと色相が近すぎて、
  「ブランド」と「この科目は重い」が同じ色になってしまうため
- **オレンジの小さい文字は使わない。** `#DB6209` は暗地では 5.1:1 だが明地では足りない。
  オレンジは面（選択チップ＝オレンジ地に白文字）と図形の線にのみ使う

**文字** ── サイズの段階を token 化する。あわせて**すべての数字に `font-variant-numeric: tabular-nums`**。
4軸の 78 / 71 / 68 が縦に揃う。CSS 2行で、いちばん効果の大きい変更。

**余白** ── 4px グリッドの `--sp-1`〜`--sp-8` に統一。

**モーション** ── `--dur-fast:120ms` `--dur:200ms` `--dur-slow:320ms`、
`--ease-out:cubic-bezier(.2,.8,.2,1)`。すべて `prefers-reduced-motion` で 1ms に落とす。
**token を先に決めないと、演出そのものが新しい「寄せ集め感」の原因になります。**

### 8.5 ページ構成

**ナビ（3項目・sticky）** ── `科目をさがす` ／ `About ラクハン` ／ `口コミを書く`（外部フォーム・外部リンクアイコン付き）

**ヘッダのロックアップ**

```
ラクハン  阪大 全学教育科目
│ Designed by GUILD
│ AI Community
```

- 左の細い縦線で2行を1つの塊にまとめ、`Designed by GUILD` をやや強く、
  `AI Community` を弱く字間広めにする。**「GUILD＝AI コミュニティ」と読ませる**のが目的。
  2行が並列の宣言に見えると「ラクハンが AI 製品」と誤読される。それは避ける
- PC は横並び、スマホは縦積み
- ブランド資料の「サービスロゴには入れない」との関係 ── この2行はロゴ本体（ロックアップ）には
  含めず、視覚的に分離した添え物として置く。資料の意図（ロゴの純度を保つ）は守る

**About ラクハン**（`web/about.html`・実ファイル。8/26 の noindex 解除後に検索へ載せるため）

1. ラクハンとは
2. なぜ作ったか
3. **数字の読み方** ── 4軸／相性／信頼度／なぜ S〜F を出さないか／なぜ教員ランキングを作らないか
4. データの出どころ ── KOAN 公開シラバス、1,112件、取れない5項目、だから口コミ
5. GUILD とは
6. **「学生団体 GUILD が運営しています。大阪大学の公式サービスではありません。」**（レッドライン。旧 B-2 完了）
7. 免責（いまのフッター文言を移設）

- 3節はワークベンチから引き揚げてくる内容。いま各セクション見出しの横にぶら下がっている
  説明文がバラつきの一因なので、ここへ集約する
- **メンバーの実名は載せません。**「GUILD メンバー6名（阪大4・京大2）」とだけ書きます。
  本人全員の同意が取れていないため。`HANDOFF.md` に名前があることと、
  公開ページに載せることは別の話です

### 8.6 一覧

- **無限スクロールを廃止。** `PAGE_SIZE = 24`。
  PC はページ番号、スマホは「もっと見る」＋「24 / 1,112件」の現在位置表示
- ページ送りで中カラムのみ先頭へ、200ms のフェード。ページ全体は飛ばさない
- 1ページ目の先頭に**「あなたに合う5件」**を、下の一覧と視覚的にはっきり分けて置く。
  **検証ずみの科目からのみ**選ぶ
- ⚠️ **本一覧の並び順アルゴリズムは一切触りません。**
  1章の「おすすめ順を検証ずみ優先に変える案」は**未決定のまま**です。
  ここで足すのは視覚的に独立した推薦枠だけで、並び順の決定を先取りしません

### 8.7 オープニング演出（約1.4秒）

ロゴは元から2本の SVG stroke path（`M26 14H48` と `M15 29H45c5 0 6.5 4 3 7L30 51`）なので、
`stroke-dasharray` / `stroke-dashoffset` で**筆順どおりに書き出せます**。
外部ライブラリは使いません。自分たちのブランド資産をそのまま動かすので、既製テンプレ感が出ません。

```
0    – 520ms   「ラ」を2画で書き出す（2画目は 80ms 遅らせる）
420  – 760ms   「クハン」を1文字ずつフェード＋3px 上へ（40ms ずつずらす）
700  – 1000ms  縦線が scaleY で伸び、Designed by GUILD / AI Community がフェードイン
1000 – 1400ms  塊がヘッダの実位置へ移動し、覆いが開いてヒーローが出る
```

守る4条件：

- **データ取得は演出の1フレーム目で発火する。** `courses.built.json` は 1.6MB（gzip 83KB）で
  実際に待ち時間がある。演出はその待ちを覆うものであって、待ちを増やすものではない。
  演出が終わってもデータが来ていなければスケルトンを出し、**それ以上待たせない**
- **再訪では流さない。** `localStorage` にタイムスタンプを持ち、24時間以内は 0.3秒の短縮版
- `prefers-reduced-motion` では完全にスキップ
- 覆いは `position:fixed` ＋ `aria-hidden="true"`。**下のページはすでに描画ずみ**にして、
  DOM もスクリーンリーダーもブロックしない

### 8.8 ファイル構成

```
web/
  index.html            ワークベンチ（外殻は build.py が注入）
  about.html            新規（同上）
  assets/tokens.css     色・文字・余白・モーションの token
  assets/app.css        レイアウト（base → md → lg → xl）
  assets/app.js         いまの JS を移設＋選択状態＋ページング
  assets/splash.js      オープニング（2ページで共用）
templates/shell.html    ヘッダ／ナビ／フッタのテンプレ（build.py が両ページへ差し込む）
```

- **新しい依存は入れません。** フロントエンドのフレームワーク（React / Vue 等）は採用しない。
  「node_modules なし・Python 標準ライブラリのみ・そのまま動く」という長所を捨てる価値がない
- `build.py`（wang）に「データを焼くついでに外殻を両ページへ注入する」役割を1つ足す
- `server.py`（wang）が `/about` と `/assets/*` を配れるようにする
- `templates/` を `web/` の外に置くのは、`CLAUDE.md` の
  「開発者用のファイルを `web/` に置かない」（`web/` は丸ごと公開される）に従うため。
  `assets/*` は実行時に配るものなので `web/` で正しい

### 8.9 今回やらないこと

- フロントエンドのフレームワーク／バンドラの導入
- **科目詳細の独立 URL・共有リンク** ── 価値は高いが今回のスコープ外。作り直し後に別タスクで
- **「私の時間割」保存** ── これは排版ではなく新機能。別タスクで
- `score.py` ／ 本一覧の並び順 ／ 4軸の重み ── 一切触らない
- サイト内投稿（`CAN_POST` は false のまま）── 受け皿が無いのに入口を出さない既存判断は正しい

### 8.10 担当

**`web/index.html` は担当表（README 7章）で松下さんのファイルです。**
今回の変更はそのファイルのほぼ全面書き換えなので、**着手前に本人の合意を取ります。**
`feat/matsushita-mobile-ui` は現時点で main と差分ゼロなので、コード上の衝突はありません。

`build.py` `server.py` は wang のファイルなので、そのまま wang が触ります。

### 8.11 検証方法

- `tools/shots.mjs` の `VIEWS` を 390 / 768 / 1280 / 1440 の4段 ＋ About ページ ＋ ダークモードへ広げる。
  PR ごとに before/after が並ぶので、**コードが読めないメンバーもレビューに参加できる**
  （このワークフローの本来の狙いどおり）
- `tools/test_shell_inject.py`（Python 標準ライブラリ・既存 `test_reviews.py` と同じ形）
  ── 注入後の2ページでヘッダ／ナビ／フッタが完全一致すること、
  「学生団体 GUILD が運営しています」が含まれること
- `tools/test_tokens.py` ── `app.css` に token を経由しない裸の hex が残っていないこと
  （既存の `test_*.py` に名前を揃える）
- 手動 ── `prefers-reduced-motion` ／ ダークモード ／ 再訪時に演出が流れないこと

---

*数値はすべて 2026-08-18 時点の `data/courses.json`（1,112件）を実際に採点して出したものです。*

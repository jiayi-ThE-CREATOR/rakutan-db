# このリポジトリで作業する AI への指示

阪大の全学教育科目の「重さ」を出すサービス **ラクハン**。GUILD の6人プロジェクト（阪大4名＋京大2名）。
締切は **2026/9/2（秋冬学期の履修登録開始）**、公開目標 **8/26**。

このプロジェクトは**バトンリレー型**です。一人の作業結果を次の人が受け取って進めます。
そのため、AI にも人にも同じ規律を求めます。

---

## 最重要：タスクが終わったら、頼まれなくても「引き継ぎ4項目」を出す

**ユーザーから指示されなくても、区切りがついた時点で必ず次の4項目を出力してください。**

1. **何をしたか** ―― コマンド1行で再現できる「動く状態」として書く
2. **何をしていないか** ―― 既知の穴・未確認のこと。**ここを省くと次の人が同じ場所を掘り返す**
3. **次の人が最初にやること** ―― 考えさせない。コピペで動き出せる形で
4. **今回踏んだ罠** ―― 同じ穴に二度落ちるのを防ぐ。**時間を一番節約するのはここ**

### 出力先

- `HANDOFF.md` の**先頭**（ルールの直下）に追記する。新しいものが上
- 同時に、Discord にそのまま貼れるテキストとしてユーザーに渡す

### 例外なく適用される場面

- 実装・調査・修正・設計変更、いずれでも区切りがついたら書く
- **前の担当者がこれを書かずに渡してきた場合は、こちらで補って書く**
- 「まだ途中だから」は理由になりません。途中なら「② 何をしていないか」に書けば済みます

見本は `HANDOFF.md` の「① スクレイパ v0（wang→政岡）」の項。
そこには**「充足率10%」という数字がパーサのバグ由来だった**経緯も残っています。
書き残さなければ、次の人が同じ誤判断をしていました。

---

## このプロジェクトで踏んではいけない線

| やらないこと | 理由 |
|---|---|
| `scrape/fetch.py --delay` を 2秒より短くする | 45分を惜しんで失うもの（大学からの停止要請）の方が大きい。**このプロジェクトを一夜で終わらせうる唯一のリスク** |
| 過去問そのものを載せる | 著作権は出題した教員にある。載せるのは「試験の傾向」まで |
| 教員を軸にした集計・並び替え・検索を作る | 担当教員名は**シラバスの事実として載せる**（コマの特定に必須）。ただし「教員別ランキング」「この先生の他の科目」を作った瞬間、事実の並記が人物評価に変わる。**評価の対象は科目のみ**。詳細は README「教員名の扱い」 |
| 総合値が出せない科目に、残った軽い軸だけで値を付ける | 一番重い軸が測れていないのに「軽め」が出る。`COVERAGE_MIN`（既定0.60）を下回る科目は総合値を出さず「情報不足」にする |
| 単一の総合スコア（S〜F や100点満点）を見出しにする | 一文字に潰すと根拠を提示できなくなる。数字が外れた瞬間に信用を失う |
| 欠損値を平均で埋める | 「情報が無い」ことは情報。信頼度と未取得項目を必ず画面に出す |
| 中身が薄いまま公開日（8/26）を優先する | 8/26 は自分たちで決めた日で、外部との約束ではない。約束は **9/2（履修登録開始）** だけ。最初に見た人は戻ってこないので、**基準を満たさなければ公開を遅らせる**。判定基準は ROADMAP.md 5章 |
| 開発者用のファイルを `web/` に置く | `web/` 配下は Cloudflare Pages にそのまま公開される。内部向けのものは `tools/` へ（`progress.html` を移動済み） |
| 他人の担当ファイルを直接いじる | 1ファイル1オーナー（担当表は README 7章） |

## 設計上、動かしてはいけない前提

- **API-first** ―― 画面は `/api/courses` を叩くだけ。LINE Bot も同じ API を使う
- **モバイルファースト** ―― 履修登録はスマホでやる。PCで綺麗でも意味がない
- **ログイン不要** ―― 検索・閲覧に登録を要求しない。これが最大の強み
- **空の検索窓を最上部に置かない** ―― 「楽なやつ」を探しに来た学生は検索語を持っていない。入口は「空きコマグリッド」
- **数字は API の実データからテンプレートで組む** ―― LINE の応答で AI に数字を書かせない。誤答は単位事故になる

---

## 事実として確認済みのこと（調べ直さなくてよい）

- KOAN 外部公開シラバスは**ログイン不要**。共通教育科目 2026年度＝**1,112件**
- 詳細ページに**成績評価の内訳（%）が構造化テーブル**で入っている
- KOAN から**取れない**のは 定員／レポート本数／字数／時間外学習の時間／毎回小テストの有無 の5つ
  → **だから口コミはこの穴を埋めるためだけに聞く**。取得と口コミは重複せず互いの穴を埋める
- 口コミの設問は **9問**（2026-08-20 更新）―― 科目／学部／履修時の学年／出席／授業中の課題／
  授業外の課題／テスト（有無・持ち込み・難易度1〜10）／レポート（有無・語数）／一言。
  **以前ここに書いていた「4タップ」ではなくなっている。** 仮フォーム（入口A）で実際に
  36件集まったので、タップ数よりも「採点に効く値が取れるか」を優先した。学部と履修年次は
  上の5つとは別枠で、**同じ科目でも学部と学年で重さが違う**ため必須で聞く
- 口コミの入口は2つあり、**設問と保存する形は必ず揃える**。ずれると `reviews.py` が
  読み落とす行ができる
  - **入口A**（`tools/ingest_reviews.py`）―― 公開前に第一手のデータを集めるための仮フォーム。
    現在の36件はここ由来。学部・学年の設問は無いので、その2つは `null` で入る
  - **入口B**（`server.py` の `POST /api/reviews` ＋ `web/index.html` のシート）―― 公開後の本番の投稿口。
    ただし**静的ホスティングでは `CAN_POST=false` で入口ごと隠れている**（受け皿が無いため）
- KOAN の「評価方法」は固定語彙ではなく**自由記述**（「態度（積極性や協調性等）」「Class debate」「理解」が実在）

検索パラメータ・ページング・詳細URLの実測値は `scrape/koan.py` の docstring にあります。

---

## 読む順番

1. この `CLAUDE.md`（今読んでいるもの）
2. `HANDOFF.md` ―― 直前の人が何を残したか
3. `README.md` ―― 設計の理由、チーム分担、進め方

# 次の版に載せるもの（仮置き場）

**ここは正本ではない。** 出したあとの正本は `web/assets/version.js` の `RELEASES` ひとつだけ。
ここに置くのは「載せると決まったが、まだ本番の版に入っていないもの」だけ。

## 使い方

1. 本番に何か出すとき、版に載せるかを利用者に聞く（判定は `CLAUDE.md`「版に載せるかの判定」）
2. 「載せる」になったら、下の一覧に1行足す
3. **毎週水曜**、溜まったものを `RELEASES` の先頭に1件の版としてまとめて移し、この一覧を空にする
   - `new` が1件でもあれば MINOR（1.2 → 1.3）、1件も無ければ PATCH（1.2 → 1.2.1）
   - **1件も無い週は版を切らない**（日付だけ進んだ空の版を出さない）
4. 移したら `node tools/test_version.mjs` を通す

初回は **2026-10-07（水）**。9月のあいだは重要なものだけを、溜まった時点で出す。

書き方（tag は `new` / `improve` / `fix` の3つだけ）:

```
- [new] 〜〜（2026-09-05 本番）
```

利用者が読む文なので、内部の言い方（リファクタ・CI・API 名）は書かない。
**「その人の画面で何が変わったか」だけ**を書く。

---

## 次の水曜に出すもの

- [new] 選んだ条件をまとめて戻せる「リセット」を置きました。学部・学年もふくめて一度に戻せます（スマホは学期の下、PC は「絞り込み」の右）
- [improve] 条件のボタンを3列に揃え、押したボタンと自動で含まれるボタンを色で見分けられるようにしました（2026-09-11 本番）
- [fix] 「あなたに合う」に出た科目が、すぐ下の一覧にも二重で出ていたのを直しました（2026-09-11 本番）
- [new] マイページの時間割のコマを押すと、その科目の詳細（重さの内訳）が開くようになりました。時間割から外すときは、コマの右下の ✕ を押してください
- [improve] 時間割のコマに担当教員を表示しました（科目名・担当教員・時間割コードの順）
- [new] 配点のつまみと条件に「発表」を足しました。「発表なし」を押すと、発表が成績に入らない授業だけを探せます（2026-09-16 本番）
- [improve] 発表を、レポートとは別の重さとして判定するようにしました。これまでは発表もレポートとして数えていたため、発表のある授業の重さが実際とずれていました。あわせて「レポートのみ」は発表のある授業を含まなくなり、多くの授業で「軽め・標準・やや重め・重め」の判定が変わっています（2026-09-16 本番）
- [new] 授業内容でさがせるようになりました。「何の話？」から歴史・AI・食などのタグを選ぶと、当てはまる科目だけが残ります。科目カードにもタグが出て、押すとそのタグでしぼれます（2026-09-17 本番）
- [improve] トップの見出しと説明を、履修登録で何が分かるかと、授業内容でもさがせることが伝わる形に変えました（2026-09-04 本番）

> `new` が1件あるので、次に切る版は MINOR（1.2 → 1.3）。
> 上2件は 2026-09-11 に先に本番へ出ているが、版としてはまだ知らせていないので
> ここに置く（判定は CLAUDE.md。リセット＝①できることが増えた、
> 3列＋濃淡＝④見た目の改善、重複＝③本番に出ていた不具合の直し）。
> マイページのコマ＝②既存の操作の結果が変わる（押した先が「外す」から
> 「詳細」に変わり、外す口が ✕ へ移った）＋①できることが増えた。
> **外し方が変わることは黙って出せない**ので、9月の重要度の門も通す。
> 担当教員の表示は単独なら④で9月は貯める側だが、同じ変更なので一緒に出す。
> 授業内容タグ＝①できることが増えた（9月の門も通す：本人判断 2026-09-16「載せる・new」）。
> トップの見出し・副題（PR #107・#109）は単独なら⑧文言の微修正だが、副題が先に「授業内容」を約束していたので、
> 授業内容が画面に出るこの版で一緒に知らせる約束（2026-09-04）。
